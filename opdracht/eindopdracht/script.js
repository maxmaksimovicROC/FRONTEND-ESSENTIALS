let name = prompt("Wat is je naam")

let email = prompt("Vul je email in")

let age = prompt("Hoe oud ben je")

let woonplaats = prompt("Vul je woon plaats in")



if(age == 16)
    {
        document.body.style.background = "#ff9900"
    }

if(age == 17)
    {
        document.body.style.background = "#0000FF"
    }
if(age == 18)
    {
        document.body.style.background = "#ffcc00"
    }


if(age == 19)
    {
        document.body.style.background = "#008000"
    }
if(age >= 20)
    {
        document.body.style.background = "#cc66ff"
    }
if(age <= 15)
    {
        document.body.style.background = "#cc66FF"
    }

















